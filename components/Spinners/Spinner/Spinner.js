
const Spinner = ({ className }) => {
    return (
        <div className={`h-3 w-3 dark:border-white border border-b-[transparent] dark:border-b-[transparent] rounded-full spin-360 ${className}`}>
        </div>
    )
}

export default Spinner
 