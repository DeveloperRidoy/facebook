

const Spacer = ({ className }) => {
    return (
      <div
        className={`border-t border-gray-300 dark:border-gray-600 w-full my-1 ${className}`}
      ></div>
    );
};

export default Spacer;